<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aaa.wisewordsinlife">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".attitudeactivites.SecondFullImage"
            android:parentActivityName=".attitudeactivites.AttitudeQActivity" />
        <activity
            android:name=".attitudeactivites.AttitudeQActivity"
            android:parentActivityName=".maindashboard.DashbActivity" />
        <activity android:name=".maindashboard.DashbActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".wiseactivities.FullScreenImage"
              android:parentActivityName=".wiseactivities.MainActivity"/>
        <activity
            android:name=".wiseactivities.MainActivity"
            android:parentActivityName=".maindashboard.DashbActivity" />
    </application>

</manifest>